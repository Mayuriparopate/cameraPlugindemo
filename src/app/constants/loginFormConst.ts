export const Login = {
    email: {
        required: "Email is required",
        email: "Email is invalid"
    },
    password: {
        required: "Password is required",
        maxlength: "Password should be 5 character long"
    }
}